<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_dialog"
    android:layout_width="@dimen/dialog_width"
    android:layout_height="@dimen/dialog_height"
    android:layout_gravity="center"
    android:background="@drawable/shape_dialog"
    android:gravity="center"
    >

    <ImageView
        android:id="@+id/iv_pic_loading"
        android:layout_width="@dimen/ic_loading_width"
        android:layout_height="@dimen/ic_loading_height"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:src="@drawable/pic_loading" />

    <com.example.iptv.utils.RobotoMediumTextView
        android:id="@+id/tv_waiting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/iv_pic_loading"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:paddingTop="@dimen/margin_smaller"
        android:text="@string/waiting"
        android:textColor="@color/text_title_white"
        android:textSize="@dimen/text_size_title" />

</RelativeLayout></layout>